import React from "react";
import "./chalkboardstyle.css";

const chalkboard = () => {
  return (
    <section id="letterBox">
      <p id="chalkText"> Welcome to the Letter Interval Application. Press start to start letter interval (entire alphabet). Press buttons below to change letter sets (press stop interval in between sets), use input for custom letter sets, this app is a work in progress ;)</p>
      <h1 class="bigLetters" id="demo3">
        {" "}
      </h1>
    </section>
  );
};

export default chalkboard;
